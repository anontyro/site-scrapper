{
  "name": "meetup-attendee-scanner",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "clean": "rm -rf dist && mkdir dist",
    "start": "npm run clean && npm run build && node dist/index.js"
  },
  "author": "alexwilkinson@gmail.com",
  "license": "MIT",
  "dependencies": {
    "preact": "^10.2.1",
    "puppeteer": "^2.0.0"
  },
  "devDependencies": {
    "@types/puppeteer": "^2.0.0",
    "webpack": "^4.41.5",
    "webpack-cli": "^3.3.10",
    "typescript": "^3.7.4",
    "ts-loader": "^6.2.1"
  }
}
